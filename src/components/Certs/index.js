import { Container } from "react-bootstrap";

const Certs = () => {
    return(
        <Container>
            <h1>
                Certificates
            </h1>
        </Container>
    )
};

export default Certs;